"use client";

import { SidebarFull } from "@/components/SidebarFull";

/** Projects list layout sidebar (collapsible). Project detail uses `SidebarDetailCollapsible` inside the page. */
export function Sidebar() {
  return <SidebarFull />;
}
